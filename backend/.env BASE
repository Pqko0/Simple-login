MONGOURI=mongodb://127.0.0.1:27017/backend
# Mongoose connection link
PORT=5858
# Express port
SECRET=dont share Mongoose
#Encryption key !!DO NOT SHARE!! ( Make sure its bigger than 20 characters )
REGISTER=TRUE
#TRUE == Accepting people to be registered || FALSE == Not allowing to register
EMAIL=FALSE
#TRUE == Show emails in user list GET /users || FALSE == Not showing emails in user list GET /users
REDIRECT_SUCCESS="/dashboard"
#Redirect when user logged in successfully


# Email SMTP, PORT, USER, PASS
EMAIL_SMTP=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=